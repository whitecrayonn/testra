# Audit Snapshots

> ⚠️ **Point-in-time snapshots, not living documentation.** Each file records the
> state of a specific audit/remediation session and goes stale quickly. For the
> current architecture and status, start from `docs/BIBLICAL_TESTRA.md` and
> `../../HANDOVER_CLAUDE.md`. Verify any claim here against the code before acting
> on it.

## 2026-07-31 — Code audit and P0 remediation

Read in this order; each supersedes the previous where they conflict:

1. [`TESTRA_CODE_AUDIT_REPORT.md`](TESTRA_CODE_AUDIT_REPORT.md) — the audit that found the issues
2. [`P0_VERIFICATION_REPORT.md`](P0_VERIFICATION_REPORT.md) — first remediation round
3. [`P0_CLOSEOUT_REPORT.md`](P0_CLOSEOUT_REPORT.md) — second remediation round (newest)

Supporting artifact:

- [`rls_table.md`](rls_table.md) — RLS policy coverage table generated by `scripts/extract_rls.mjs` + `scripts/generate_rls_table.mjs`

### Corrections applied after the closeout report

Found while independently verifying the reports' claims (see `HANDOVER_CLAUDE.md` §7):

- The queue worker-mode RLS bypass leaked across pooled connections (`SET` → `SET LOCAL`).
- `queue.DeleteOldCompleted` never worked (timestamp/interval type mismatch); pre-existing bug, now fixed.
- `TestDequeueConcurrency` did not run in CI — the `go` job now provisions PostgreSQL so it does.
