ENV=development
PORT=8080
DATABASE_URL=postgres://testra:testra@localhost:5432/testra?sslmode=disable
MIGRATIONS_PATH=apps/api/migrations
JWT_ISSUER=testra
JWT_AUDIENCE=testra-api
JWT_PRIVATE_KEY_FILE=
JWT_PUBLIC_KEY_FILES=
JWT_EXPIRY_MINUTES=15
REFRESH_EXPIRY_DAYS=30
REFRESH_ABSOLUTE_DAYS=90
REDIS_ADDR=localhost:6379
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noreply@testra.local
SMTP_USERNAME=
SMTP_PASSWORD_SECRET=SMTP_PASSWORD
IDEMPOTENCY_KEY_TTL_MINUTES=1440
CORS_ALLOWED_ORIGINS=http://localhost:3000
ML_SERVICE_URL=http://localhost:8000
# Must match ML_API_KEY in apps/ml's environment; sent as X-API-Key on every
# call to the ML service. Leave blank in local dev if the ML service also
# leaves ML_API_KEY unset.
ML_API_KEY=
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
